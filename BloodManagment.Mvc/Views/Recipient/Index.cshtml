@* @model IList<RecipientDto>

@{
    ViewData["Title"] = "المستفيدين";
    Layout = "_DashboardLayout";
}

<h3 class="mb-4">🏥 قائمة المستفيدين</h3>

<table class="table table-hover text-center align-middle">
    <thead class="table-dark">
        <tr>
            <th>الكود</th>
            <th>الاسم</th>
            <th>النوع</th>
            <th>عدد الطلبات</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.RecipientCode</td>
                <td>@item.FullName</td>
                <td>@item.Gender</td>
                <td>
                    <span class="badge bg-danger">
                        @item.BloodRequestsCount
                    </span>
                </td>
            </tr>
        }
    </tbody>
</table> *@